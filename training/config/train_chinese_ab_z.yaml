# A/B 实验组：只开 Router Z-Loss（z = logsumexp(router_logits)² 正则），训练 1500 步。
# 与 ab_base 的唯一差异：z_loss_weight=1e-4（qk_norm 保持关闭）。
extends: train_chinese.yaml

out_dir: out/chinese-data2-ab-z
enable_early_stop: false
wandb_run_name: ab-z-1500
max_iters: 1500
z_loss_weight: 0.0001