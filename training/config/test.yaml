# 快速冒烟配置：和 train_chinese.yaml 同架构，但模型更小、步数更少，秒级验证。
extends: train_chinese.yaml

out_dir: out/test
eval_interval: 20
eval_iters: 10
log_interval: 5
batch_size: 16
n_layer: 2
n_head: 2
n_embd: 32
dropout: 0.0
n_experts: 2
n_top_k: 1
csa_topk: 2
max_iters: 50
lr_decay_iters: 50
warmup_iters: 10
